#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int solve(string s,int n)
{
    int ans = n;
    map<int,int> mp;

    for(auto x:s) mp[x]++;

    for(int )
}

int main()
{
    ll n;
    cin>>n;

    string s;
    cin>>s;

    cout<<solve(s,n)<<endl;
}